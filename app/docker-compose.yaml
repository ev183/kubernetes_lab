services:
  flask-app:
    build: .                  # build from the Dockerfile in this directory
    ports:
      - "5000:5000"           # host:container
    volumes:
      - ./app.py:/app/app.py  # hot reload — changes reflect without rebuilding
    environment:
      - FLASK_ENV=development
      - FLASK_DEBUG=1